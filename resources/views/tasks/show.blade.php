@extends('layouts.app')
@section('title',$task->title)
@section('content')<section class="page narrow shell"><a class="back" href="{{ route('projects.show',$project) }}">← Back to {{ $project->title }}</a><article class="task-detail reveal"><div class="detail-top"><span class="status-pill {{ $task->status }}">{{ ucwords(str_replace('_',' ',$task->status)) }}</span><span class="priority {{ $task->priority }}">{{ ucfirst($task->priority) }} priority</span></div><h1>{{ $task->title }}</h1><p class="detail-copy">{{ $task->description ?: 'No notes were added to this task.' }}</p><dl><div><dt>Project</dt><dd>{{ $project->title }}</dd></div><div><dt>Due date</dt><dd>{{ $task->due_date ? $task->due_date->format('F j, Y') : 'Not set' }}</dd></div><div><dt>Created</dt><dd>{{ $task->created_at->format('M j, Y') }}</dd></div></dl><div class="detail-actions"><a class="btn" href="{{ route('projects.tasks.edit',[$project,$task]) }}">Edit task</a><form method="POST" action="{{ route('projects.tasks.destroy',[$project,$task]) }}" data-confirm="Delete this task?">@csrf @method('DELETE')<button class="danger-btn">Delete</button></form></div></article></section>@endsection
